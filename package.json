{
  "name": "diatomic-molecule-simulation-widget",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-wasm": "wasm-pack build wasm-crate --target web --out-dir ../public/wasm",
    "start": "npm run build-wasm && npm run dev"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
