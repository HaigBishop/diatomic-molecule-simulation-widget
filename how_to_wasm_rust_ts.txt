
(install npm at node js download)

npm create vite@latest diatomic-molecule-simulation-widget --template vanilla-ts

cargo install wasm-pack

mkdir wasm-crate

cd wasm-crate

cargo init --lib

wasm-pack build --target web --out-dir ../public/wasm

npm install

npm run dev



///////// MAIN STUFF

cd wasm-crate

// Compile Rust wasm
wasm-pack build --target web --out-dir ../public/wasm

cd ..

// Run app
npm run dev

(added commands to scripts in package.json)

// Compile Rust wasm AND run app
npm run start 


